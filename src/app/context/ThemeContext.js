// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#000000', // Primary color set to #000
    },
    secondary: {
      main: '#444444', // Secondary color set to #444
    },
  },
});

export default theme;
